# SOTA reference — MOVED

> **This file is a stub. The living SOTA / bibliography reference is now
> [`docs/BIBLIOGRAPHY.md`](BIBLIOGRAPHY.md).**

An earlier version of this file duplicated the per-task published-score tables and drifted out of
date (it still described RadioML 2018.01a as "blocked" and MCLDNN as a run "in flight", both of which
have since landed). To keep a single source of truth, all of that content — the cross-paper
overall-accuracy tables, the dataset/model inventory, the per-model reproduction audit, and the
`from_paper` literature figures — now lives in `docs/BIBLIOGRAPHY.md` (Part A bibliography,
Part B reproduction audit, Part C what's missing).

For dataset licensing specifically, see `docs/LICENSES.md`. For the normative evaluation protocol
(metrics, splits, verification tiers), see `docs/EVALUATION_PROTOCOL.md`.
