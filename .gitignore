# --- Données & artefacts lourds : JAMAIS en git (voir CLAUDE.md) ---
# NB: ancrés à la racine (/) pour ne PAS masquer le sous-package rfbench/data/.
# Les données brutes restent bloquées partout par les règles d'extension ci-dessous + le garde-fou CI.
/data/
/datasets/
/checkpoints/
*.pt
*.ckpt
*.pth
*.h5
*.hdf5
*.npy
*.npz
*.bin
*.sigmf-data
*.tar
*.tar.gz
# résultats bruts (le result.json de soumission est versionné explicitement si besoin)
outputs/
logs/
wandb/
runs/

# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# --- Claude Code artefacts (worktrees, etc.) : jamais en git ---
.claude/

# --- Env & secrets ---
.env
*.local

# --- OS / éditeurs ---
.DS_Store
*.swp
.idea/
.vscode/
