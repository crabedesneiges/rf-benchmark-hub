# @package regime
# The `full_finetune` regime (D5): a foundation model fine-tuned end-to-end on the task.
# Adapter: rfbench.regimes.FullFinetuneAdapter — a thin pass-through to Model.forward
# (the fine-tuning that produced the weights is M6, out of scope for the eval harness).
name: full_finetune
# k is meaningful only for few_shot; null here (mirrors RegimeSpec's coupling).
k: null
